import React from "react";
import "../styles/globals.css";

const ctaBtn = (props) => {
  return (
    <div>
      <button className="ctaBtn">{props}</button>
    </div>
  );
};

export default ctaBtn;
